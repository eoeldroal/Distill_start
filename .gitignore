# 비밀값. .env.example만 추적한다.
.env
*.pem
*.key

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# 재계산이 싼 대용량 산출물. 스크립트가 다시 만들 수 있으므로 이력에 남기지 않는다.
/Experiment/BranchDev/outputs/emb_*.npy
/Code/verl_smoke/data/
/Code/verl_smoke/outputs/
# prepare_math.py 가 다시 만든다
/Code/rl/data/
# hydra 가 실행마다 새로 쓴다 (실행 위치에 따라 최상단에도 생긴다)
/Code/rl/outputs/
/outputs/
/Code/rl/logs/
/Code/rl/ckpt/

# 훈련 산출물. 최상단에만 걸어 둔다. vendor한 verl 안에 같은 이름의
# 소스 디렉토리가 있어서(tests/utils/ckpt/) 전역 규칙을 쓰면 진짜 파일이 빠진다.
/checkpoints/
/ckpt/
/wandb/
/ray_results/
*.ckpt
*.safetensors
*.bin
*.pt

# 에디터, OS. 우리 최상단만 걸고, vendor한 verl의 것은 upstream 그대로 둔다.
/.vscode/
/.idea/
.DS_Store
